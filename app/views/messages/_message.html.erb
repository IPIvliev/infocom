<dl class="dl-horizontal">
    <dt><%= Russian::strftime(message.created_at, "%d %B %Y %H:%M") %></dt>
    <dt>
        <% if message.answer == true %>
            <%= link_to 'Обработана'.html_safe, answer_message_path(message), :class => "green", method: :put %>
        <% else %>
            <%= link_to 'Новая заявка', answer_message_path(message), :class => "red", method: :put %>
        <% end %>
    </dt>
    <dt>
        <%= link_to "Удалить", message, method: :delete, :class => "red", data: { confirm: "Вы уверены?" } if current_user.status == 3 %>
    </dt>
    <dd>
        <%= message.name %>
    </dd>
    <dd>
        <%= message.email %>
    </dd>
    <dd>
        <%= message.phone %>
    </dd>
    <dd>
        <%= message.text %>
    </dd>
</dl>